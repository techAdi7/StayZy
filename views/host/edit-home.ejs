<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>
  </head>

  <body class="bg-gradient-to-br from-blue-50 via-white to-pink-50 min-h-screen flex flex-col">
    <%- include('../partials/nav') %>
      <main class="flex-1 flex flex-col items-center justify-center">
        <div class="bg-white rounded-2xl shadow-2xl p-10 text-center max-w-lg w-full">
          <h1 class="text-3xl font-extrabold text-blue-600 mb-8">
            <%= editing ? "Edit" : "Register" %> Your Home on AirBnB
          </h1>
          <form action="<%= editing ? '/host/edit-home' : '/host/add-home' %>" method="post"
            enctype="multipart/form-data" class="flex flex-col space-y-6">
            <input type="hidden" name="id" value="<%= editing ? home._id : '' %>" />
            <input type="text" name="homeName" placeholder="Enter the name of your house"
              class="px-5 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-lg"
              value="<%= editing ? home.homeName : '' %>" required>
            <input type="text" name="pricePerNight" placeholder="Enter Price per night"
              class="px-5 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-lg"
              value="<%= editing ? home.pricePerNight : '' %>" required>
            <input type="text" name="location" placeholder="Enter the location"
              class="px-5 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-lg"
              value="<%= editing ? home.location : '' %>" required>
            <input type="text" name="rating" placeholder="rating"
              class="px-5 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-lg"
              value="<%= editing ? home.rating : '' %>" required>

            <input type="file" name="photo" multiple accept="image/jpeg, image/png, image/jpg"
              class="w-full px-4 py-2 border border-gray-300 rounded-md bg-white text-gray-700" <%=editing ? ''
              : 'required' %>>


            <textarea name="description" placeholder="Describe your home"
              class="px-5 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400 text-lg"
              required><%= editing ? home.description : '' %></textarea>

            <input type="submit" value="<%= editing ? 'Update Home' : 'Add Home' %>"
              class="px-8 py-3 bg-blue-600 text-white rounded-xl shadow hover:bg-blue-700 transition font-bold cursor-pointer">
          </form>
        </div>
      </main>
      <footer class="bg-white py-6 mt-10 shadow-inner">
        <div class="container mx-auto text-center">
          <p class="text-gray-500 text-sm">&copy; 2025 Stayzy, Inc. All rights reserved.</p>
        </div>
      </footer>
  </body>

</html>